# Joshua Layfield AI Artifacts

Personal GitHub Pages site for AI-built artifacts captured in Open Brain.

The homepage is a static `index.html` page with one card per artifact. `_data/artifacts.yml` mirrors the same artifact list so future updates have a structured source to work from. Each card includes the artifact purpose, outputs, process, result, and public links where appropriate.
